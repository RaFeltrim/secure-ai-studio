# 🛡️ SECURE AI ENGINE CONFIGURATION

## SYSTEM CONFIGURATION FILE

# Core Engine Settings
ENGINE_VERSION = "1.0.0"
MAX_CONCURRENT_JOBS = 4
DEFAULT_TIMEOUT = 300  # seconds
MEMORY_LIMIT = "8GB"
CPU_CORES = 4

# Security Settings
AIR_GAP_MODE = True
NETWORK_ISOLATION = True
FILE_ENCRYPTION = True
ENCRYPTION_KEY_LENGTH = 256
ACCESS_LOGGING = True
AUDIT_LEVEL = "HIGH"

# Image Generation Settings
IMAGE_FORMATS = ["PNG", "JPEG", "BMP", "TIFF", "WEBP"]
MAX_IMAGE_SIZE = (4096, 4096)
DEFAULT_QUALITY = 95
BATCH_PROCESSING = True
MAX_BATCH_SIZE = 50

# Video Generation Settings
VIDEO_FORMATS = ["MP4", "AVI", "MOV", "GIF"]
MAX_VIDEO_RESOLUTION = (3840, 2160)
FRAME_RATES = [24, 30, 60]
MAX_VIDEO_DURATION = 300  # seconds
CODEC_PREFERENCE = "H.264"

# Brand Protection Settings
WATERMARK_ENABLED = True
WATERMARK_OPACITY = 0.7
WATERMARK_POSITION = "BOTTOM_RIGHT"
COPYRIGHT_EMBEDDING = True
METADATA_INJECTION = True

# Storage Settings
OUTPUT_DIRECTORY = "/opt/secure-ai-studio/output"
TEMP_DIRECTORY = "/tmp/secure-ai-temp"
BACKUP_ENABLED = True
BACKUP_LOCATION = "/opt/secure-ai-studio/backup"
RETENTION_PERIOD = 90  # days

# Performance Settings
CACHE_ENABLED = True
CACHE_SIZE = "2GB"
PRELOAD_MODELS = True
OPTIMIZATION_LEVEL = "HIGH"
THREAD_POOL_SIZE = 8

# Logging Settings
LOG_LEVEL = "INFO"
LOG_FORMAT = "%(asctime)s - %(name)s - %(levelname)s - %(message)s"
LOG_ROTATION = "daily"
LOG_RETENTION = 30  # days

# User Interface Settings
UI_THEME = "dark"
LANGUAGE = "en-US"
AUTO_SAVE_INTERVAL = 300  # seconds
NOTIFICATION_LEVEL = "NORMAL"

# Model Settings
MODEL_CACHE_DIR = "/opt/secure-ai-studio/models"
DEFAULT_IMAGE_MODEL = "stabilityai/stable-diffusion-2-1"
DEFAULT_VIDEO_MODEL = "damo-vilab/text-to-video-ms-1.7b"
MODEL_DOWNLOAD_ENABLED = False  # Always offline
CUSTOM_MODEL_SUPPORT = True

# Export Settings
EXPORT_FORMATS = ["PNG", "JPEG", "MP4", "PDF"]
EXPORT_QUALITY_OPTIONS = ["LOW", "MEDIUM", "HIGH", "LOSSLESS"]
EXPORT_WATERMARK_REQUIRED = True
EXPORT_LOGGING = True

# Compliance Settings
DATA_RETENTION_POLICY = "STRICT"
PRIVACY_COMPLIANCE = True
GDPR_READY = True
HIPAA_COMPLIANT = False  # Not healthcare focused
EXPORT_CONTROLS = True